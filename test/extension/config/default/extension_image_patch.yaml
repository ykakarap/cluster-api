apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-extension
  namespace: system
spec:
  template:
    spec:
      containers:
      - image: gcr.io/kakaraparthy-devel/test-extension-amd64:dev
        name: extension
